config_data: |
  extends: default
  rules:
    line-length:
      max: 120
    document-start:
      present: false
    braces:
      forbid: false
      min-spaces-inside: 1
      max-spaces-inside: 1
